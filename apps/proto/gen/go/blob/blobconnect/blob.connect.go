// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: blob/blob.proto

package blobconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	blob "github.com/aidan-neel/shulker/apps/proto/gen/go/blob"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// BlobServiceName is the fully-qualified name of the BlobService service.
	BlobServiceName = "blob.BlobService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// BlobServicePutBlobProcedure is the fully-qualified name of the BlobService's PutBlob RPC.
	BlobServicePutBlobProcedure = "/blob.BlobService/PutBlob"
)

// BlobServiceClient is a client for the blob.BlobService service.
type BlobServiceClient interface {
	PutBlob(context.Context, *connect.Request[blob.PutBlobRequest]) (*connect.Response[blob.PutBlobResponse], error)
}

// NewBlobServiceClient constructs a client for the blob.BlobService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewBlobServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) BlobServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	blobServiceMethods := blob.File_blob_blob_proto.Services().ByName("BlobService").Methods()
	return &blobServiceClient{
		putBlob: connect.NewClient[blob.PutBlobRequest, blob.PutBlobResponse](
			httpClient,
			baseURL+BlobServicePutBlobProcedure,
			connect.WithSchema(blobServiceMethods.ByName("PutBlob")),
			connect.WithClientOptions(opts...),
		),
	}
}

// blobServiceClient implements BlobServiceClient.
type blobServiceClient struct {
	putBlob *connect.Client[blob.PutBlobRequest, blob.PutBlobResponse]
}

// PutBlob calls blob.BlobService.PutBlob.
func (c *blobServiceClient) PutBlob(ctx context.Context, req *connect.Request[blob.PutBlobRequest]) (*connect.Response[blob.PutBlobResponse], error) {
	return c.putBlob.CallUnary(ctx, req)
}

// BlobServiceHandler is an implementation of the blob.BlobService service.
type BlobServiceHandler interface {
	PutBlob(context.Context, *connect.Request[blob.PutBlobRequest]) (*connect.Response[blob.PutBlobResponse], error)
}

// NewBlobServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewBlobServiceHandler(svc BlobServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	blobServiceMethods := blob.File_blob_blob_proto.Services().ByName("BlobService").Methods()
	blobServicePutBlobHandler := connect.NewUnaryHandler(
		BlobServicePutBlobProcedure,
		svc.PutBlob,
		connect.WithSchema(blobServiceMethods.ByName("PutBlob")),
		connect.WithHandlerOptions(opts...),
	)
	return "/blob.BlobService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case BlobServicePutBlobProcedure:
			blobServicePutBlobHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedBlobServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedBlobServiceHandler struct{}

func (UnimplementedBlobServiceHandler) PutBlob(context.Context, *connect.Request[blob.PutBlobRequest]) (*connect.Response[blob.PutBlobResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("blob.BlobService.PutBlob is not implemented"))
}
